{
  "name": "creator-notes-html-renderer",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build:backend": "bun build src/backend.ts --outfile dist/backend.js --target bun --external @lumiverse/spindle",
    "build:frontend": "bun build src/frontend.ts --outfile dist/frontend.js --target browser --external @lumiverse/spindle",
    "build": "bun run build:backend && bun run build:frontend",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "lumiverse-spindle-types": "0.4.74",
    "typescript": "^5.9.3"
  }
}
